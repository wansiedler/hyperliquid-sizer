# Relaxed base: gate real YAML errors (duplicate keys, syntax) without failing
# on formatting. Style findings stay warnings.
extends: relaxed
rules:
  key-duplicates: enable
  line-length: disable
  truthy:
    check-keys: false   # GitHub Actions `on:` is a key, not a truthy value
